# 虚拟键盘 — 使用说明

在直播里**显示你按了哪些键**。按顺序做下面几步即可。

---

## 1. 启动

**方式 A（推荐，不用装 Python）**  
到项目 GitHub 页的 **Releases** 下载 **`TimeZoneKeyboard.exe`**，放到任意文件夹（例如桌面上的单独文件夹），**双击运行**。程序会在**同一文件夹**下生成 **`configs`** 保存你的方案；浏览器打开 `http://localhost:8080` 即可（若未自动打开，请手动用 Chrome / Edge 打开）。

**方式 B（克隆了完整源码）**  
在仓库**根目录**双击 **`start-keyboard.bat`**，需要本机已安装 **Python**（[python.org](https://www.python.org/downloads/)，安装时勾选 **Add Python to PATH**）。

通用步骤：

1. 双击 **`TimeZoneKeyboard.exe`**（Release）或 **`start-keyboard.bat`**（源码 + Python）。
2. 等浏览器打开 `**http://localhost:8080**`；没自动开就自己用 Chrome / Edge 输入这一行打开。

**不要**双击 `index.html` 打开，否则**存不了配置**、按键也容易不正常。

---

## 2. OBS 里填什么（最容易错）


| 用途           | 地址                                                          |
| ------------ | ----------------------------------------------------------- |
| 你自己改键盘       | `http://localhost:8080`                                     |
| **OBS 浏览器源** | 页面上方 **「OBS 浏览器源」** 输入框里的**整段**（里面有 `**/overlay?config=`**） |


在 OBS：**来源 → 浏览器 → 粘贴上面第二行**，不要填首页那一串。

**宽、高**：尽量和页面顶部预览栏里的 **W、H** 一致（改完数字要点「应用」）。

---

## 3. 按键要亮起来

预览区附近要出现 **「全局按键捕获：已连接」**。  
没有这句话，游戏里按键可能不会亮——先确认 **`TimeZoneKeyboard.exe`** 或 **`start-keyboard.bat`** 已经运行过。

---

## 4. 改键与保存

- **移动 / 放大缩小**：在画面上拖键、拖键的边缘。  
- **改一颗键**：在键上**双击**。  
- **加键**：「键位」里点「添加按键」，再按键盘上对应的键。  
- **删键**：列表里点「删除」。  
- **收起下半屏**：按键盘 **F2**。

改完到 **「配置」** 里点 **保存**。想连 OBS 链接一起更新，用 **「保存并复制」** 最省事。

---

## 5. 常见问题（很短）

**OBS 里不亮？**  
先看浏览器源里是不是 **overlay** 那条链接；再右键该来源 → **刷新**。

**保存失败？**  
多半是没通过 `**http://localhost:8080`** 打开页面，请先运行 exe / bat 后再开这个地址。

**换电脑？**  
把 **`TimeZoneKeyboard.exe` 所在文件夹**整份拷走（里面的 **`configs`** 一起带上）。

**启动报错但窗口一闪没了？**  
在文件夹里打开终端，输入 `**python key_server.py`**，把黑窗里的报错截图发 GitHub Issues。

---

更细的说明见仓库 **[README.md](README.md)**；改代码见 **[CONTRIBUTING.md](CONTRIBUTING.md)**。  
作者 B 站：[无知小冒](https://space.bilibili.com/10158668)。